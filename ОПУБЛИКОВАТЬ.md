# Задача для Claude Code: опубликовать Maestro Kit на GitHub

Открой Claude Code в этой папке (`~/Desktop/maestro-kit`) и скажи: «сделай по ОПУБЛИКОВАТЬ.md».

TASK: MAESTRO-PUBLISH — первый выпуск maestro-kit
Model: Haiku (механика) · Effort: 10 минут · Никаких правок содержимого файлов.

## Шаги
1. Проверка: `ls -a` — должны быть `.claude/ templates/ library/ README.md KIT.md НАЧНИ-ЗДЕСЬ.md CHANGELOG.md LICENSE`. Git ещё не инициализирован (`git status` → «not a git repository»). Иначе — стоп, спроси человека.
2. `printf ".DS_Store\n_to_delete/\n" > .gitignore`
3. `git init -b main && git add -A && git commit -m "feat: maestro-kit 1.0.0 — conductor + wiki hybrid"`
4. Спроси подтверждение: «Создаю ПУБЛИЧНЫЙ репозиторий maestro-kit на GitHub и пушу — ок?»
   После «ок»: `gh repo create maestro-kit --public --source . --remote origin --push`
   (если `gh` не залогинен — `gh auth login`, проведи человека по шагам).
5. Самоочистка: `git rm ОПУБЛИКОВАТЬ.md && git commit -m "chore: cleanup publish note" && git push`
6. Отчёт: ссылка на репозиторий + напоминание, что команда установки из README/НАЧНИ-ЗДЕСЬ
   уже указывает на omikhaylov76-commits/maestro-kit — проверь, что имя совпало.

## Границы
Содержимое файлов не менять. Push — только после «ок». Репозиторий — ПУБЛИЧНЫЙ (решение владельца).
